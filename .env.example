# Neon Postgres connection string (get it from the Neon dashboard).
# Copy this file to .env.local and fill in the value — .env.local is git-ignored.
# Without it, the site serves the bundled data/seed.json.
DATABASE_URL=
